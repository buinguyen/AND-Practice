<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="test.app.alan.mytest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.TestApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar"
        android:supportsRtl="true">
        <activity android:name=".app.TestMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".progressdialog.ProgressDialogActivity" />
        <activity android:name=".patterns.solid.SolidPatternActivity" />
        <activity android:name=".adapter.multitype.MultiTypeAdapterActivity" />
        <activity android:name=".localize.AnotherActivity" />
        <activity android:name=".localize.LocalizeActivity" />

        <activity android:name=".dagger2.ui.homepage.HomepageActivity" />
        <activity android:name=".dagger2.ui.search.SearchActivity" />

        <activity android:name=".patterns.abstractfactory.AbstractFactoryActivity"/>
    </application>

</manifest>